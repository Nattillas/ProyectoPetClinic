Feature: Pet Types API Tests
  As a PetClinic API user
  I want to validate the Pet Types endpoint
  So that I can ensure the API returns correct pet type information

  Background:
    Given the PetClinic API is available
    And the API base URL is configured
    And the common headers are set

  @smoke @pet-types @api
  Scenario: Verify bla bla bla. ADD PET TYPE SCENARIO DEFINITION HERE
  Given  I want to do something...